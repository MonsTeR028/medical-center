<header>
    <span>
        <a href="{{ path('app_home') }}">
            <img id="logo" src="{{ asset('images/logo.png') }}" alt="MedicalCenter">
            <h1 class="league-spartan-medium">MedicalCenter</h1>
        </a>
    </span>
    <span class="header-button">
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <a href="#" class="lexend-bold">
                <span class="material-symbols-outlined">package_2</span>
                Commandes
            </a>
            <a href="{{ path('app_medicine') }}" class="lexend-bold">
                <span class="material-symbols-outlined">pill</span>
                Médicaments
            </a>
            <a href="{{ path('app_cart') }}" class="lexend-bold">
                <span class="material-symbols-outlined">shopping_cart</span>
                Panier
            </a>
            <a href="#" class="lexend-bold">
                <span class="material-symbols-outlined">person</span>
                Compte
            </a>
        {% else %}
            <a href="{{ path('app_user_register') }}" class="lexend-bold">Inscription</a>
            <a href="{{ path('app_login') }}" class="lexend-bold">Connexion</a>
        {% endif %}
    </span>
</header>
